name: Apply CLI boundary once

on:
  push:
    branches:
      - feat/boot-rebuild-backends
    paths:
      - .github/workflows/apply-cli-boundary-once.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/boot-rebuild-backends
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Apply patch
        run: python scripts/apply_cli_boot_boundary.py
      - name: Install development environment
        run: |
          python -m pip install -e .
          python -m pip install -r requirements-dev.txt
      - name: Verify focused GREEN
        run: python -m pytest tests/test_cli.py tests/test_boot_preflight.py tests/test_initramfs.py tests/test_host_safety.py tests/test_modes.py -v
      - name: Static checks
        run: |
          ruff check envycontrol.py envycontrol_boot.py tests/test_cli.py
          mypy envycontrol.py envycontrol_boot.py
      - name: Commit real change and remove helpers
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm scripts/apply_cli_boot_boundary.py .github/workflows/apply-cli-boundary-once.yml
          git add envycontrol.py
          git commit -m 'fix: fail boot preflight before cache mutation'
          git push origin HEAD:feat/boot-rebuild-backends
